// Drugi program jest szybszy od pierwszego
// nastapi przesunieci o jeden
// nie zauwazylem braku ==
// waga >= 115 && waga < 125
// ch == 'q' || ch == 'Q'
// x % 2 == 0 && x != 26
// gosc = (donacja > 1000 && donacja < 2000) ? 0 : 1
// ch >= 'a' || ch <= 'z' && ch >= 'A' || ch <= 'Z'
// 5. wyra¿enie !! jest nie poprawne wyra¿enie !(!x) moze znaczyæ tyle samo co x
// if (x < 0) {
// x *= -1
// 
#include<iostream>

using namespace std;

int main()
{
	char ch;
	int a_grade = 0;
	int b_grade = 0;
	int c_grade = 0;
	int d_grade = 0;
	int f_grade = 0;
	switch (ch) {
	case 'A':
		a_grade++;
		break;
	case 'B':
		b_grade++;
		break;
	case 'C':
		c_grade++;
		break;
	case 'D':
		d_grade++;
	default:
		f_grade;
	}
//

}